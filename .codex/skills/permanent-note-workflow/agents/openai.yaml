interface:
  display_name: "Permanent Note Workflow"
  short_description: "AI候補をBaseで承認しPermanent Noteへ昇格"
  default_prompt: "Use $permanent-note-workflow to create reviewable Permanent Note candidates and safely apply only the approved ones."
